[deploy]
startCommand = "python inject_analytics.py && streamlit run app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
