[pytest]
# Default test run excludes smoke tests (live API calls, ~$0.02-0.05/run)
# To run smoke tests: pytest -m smoke -s
pythonpath = .
addopts = -m "not smoke"

markers =
    smoke: live end-to-end tests that make real API calls (excluded from default run)
